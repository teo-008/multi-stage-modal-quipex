import { TestBed } from '@angular/core/testing';
import { StageAddBuildingConfirmationComponent } from './stage-add-building-confirmation.component';

describe('StageAddBuildingConfirmationComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [StageAddBuildingConfirmationComponent],
    }).compileComponents();
  });

  xit('should create the component', () => {});

  xit('should render the recently completed building name correctly', () => {
    // possibly test this in the container
  });
});
